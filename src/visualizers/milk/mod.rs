#[allow(warnings)]

use crate::{
    math::{self, Cplx},
    graphics,
    visualizers::VisFunc
};

pub mod rain;

pub fn draw_bubbles(prog: &mut crate::data::Program, stream: &mut crate::audio::SampleArr) {
}
